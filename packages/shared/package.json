{
  "name": "shared",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "types": "tsc --noEmit",
    "lint": "eslint 'src/**/*.{js,jsx,ts,tsx}'",
    "clean": "rm -rf node_modules && rm -rf dist && rm -rf yarn-error.log"
  },
  "dependencies": {},
  "devDependencies": {
    "@vavra7/eslint": "*",
    "typescript": "^4.9.5"
  }
}
